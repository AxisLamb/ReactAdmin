# MEMORY.md

## User Environment & Setup
- **OS**: Windows.
- **Language**: Chinese (User communicates in Chinese, e.g., "任务已完成，请关闭浏览器").
- **System Encoding**: Windows shell commands frequently output garbled text (e.g., "ϵͳҲָ·") due to locale/encoding mismatches. 
- **Node.js/npm**: Not installed or not available in the system PATH.

## Tools & Configuration
- **`agent-browser`**: Currently unavailable due to missing npm. 
  - *Manual Installation*: Requires `npm install -g agent-browser` followed by `agent-browser install`.

## Active Project Context
- **Path**: `D:\project\ReactAdmin\backend`
- **Stack**: Java / Spring.
- **Key Files**: `pom.xml`, `Dockerfile`, `docker-compose.yml`, `API_DOC.md`, `WECHAT_BOT.md`, `DEPLOY_dockercompose.md`, `prod.env`, `LICENSE`, `README.md`.
- **Key Directories**: `src/`, `target/`, `db/`, `deploy/`, `logs/`, `uploads/`, `.idea/`, `.git/`, `.codebuddy/`.

## Recent Tasks & Interests
- **Goofish (https://www.goofish.com/)**: User is investigating Goofish (Alibaba's Xianyu international / second-hand trading platform). Tasks included extracting the page title, main content, taking screenshots, and identifying all clickable interactive elements on the site.